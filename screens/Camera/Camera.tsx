import React from 'react';
import {Text, TouchableOpacity} from 'react-native';

const Camera = () => {
  
  return (
    <TouchableOpacity>
      <Text>Camera</Text>
    </TouchableOpacity>
  );
};

export default Camera;
